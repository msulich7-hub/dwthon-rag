# SPEC: CRM 2027 Phase B (Twenty parity, app module only)

**Status:** implemented  
**Constraint:** no edits to `packages/core`, `packages/ui`, `packages/ai-assistant`

## Delivered

1. **Namespace routes** — `/backend/crm_2027/people|companies|deals/[id]` redirect to customers v2/detail
2. **Local Cmd+K** — `CrmShell` + `CrmCommandPalette` (scoped hotkeys, not AppShell)
3. **Dashboard** — `/backend/crm_2027/dashboard` + `GET /api/crm_2027/dashboard`
4. **List injection** — `list-context-bar` on people/companies/deals DataTable search-trailing
5. **Deal header injection** — `deal-quick-links` (at-risk, dashboard)
6. **Perspective tableIds** — `crm_2027.people|companies|deals` documented for future perspectives UI

## Next (Phase C — upstream only)

- Global command palette slot in platform
- Calendar view primitive
- Email/calendar sync module
